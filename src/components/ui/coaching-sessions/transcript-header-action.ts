export const TRANSCRIPT_HEADER_ACTION_CLASS =
  "h-7 w-7 p-0 text-muted-foreground/50 hover:text-foreground";

// Maximize and close are desktop-only; both have mobile equivalents elsewhere
// on the page. Download has none, so it stays visible at every width.
export const TRANSCRIPT_HEADER_ACTION_DESKTOP_ONLY = "hidden md:inline-flex";
